# Production environment — used automatically by Vite when building for production.
# This file contains NO secrets and is safe to commit.
VITE_API_BASE_URL=https://api.budgetagent.myaisquad.com/api/v1
